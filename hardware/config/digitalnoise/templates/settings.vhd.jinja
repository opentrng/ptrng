library ieee;
use ieee.std_logic_1164.all;

library opentrng;
use opentrng.constants.all;

-- This file has been automatically generated with the command line:
-- $ {{ cmd }}
-- For more information look into the directory 'hardware/config/digitalnoise'.
package settings is

	-- Ring-oscillators settings
	constant T: natural := {{ rings|length-1 }};
	type len_array is array (0 to T) of natural;
	{%- set join = joiner(", ") %}
	constant RO_LEN: len_array := ({% for ring in rings %}{{ join() }}{{ ring['parameters']['length'] }}{% endfor %});

	-- Digitizer settings
	constant DIGITIZER_GEN: natural := {{ digitizer['type'] }};

end package;
